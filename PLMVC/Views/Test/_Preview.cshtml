@model PLMVC.Models.Test.PreviewTestViewModel

@*<script>
    function showFirstQuestion() {
        var lProp = $("#question-1").prop("display");
        console.log(lProp);
        lProp = "block";

    }
</script>*@

<div class="jumbotron">
    <h1>@Model.Title</h1>
    <p>To pass the test successfully you are to give @Model.MinToSuccess.ToString("0.00")% correct answers up to @Model.TimeLimit.ToString(@"hh\:mm\:ss").</p> 
    <p>Good luck:)</p>
    @Ajax.ActionLink("Back", "ShowLastTests", "Test", new AjaxOptions{UpdateTargetId = "main_container", LoadingElementId = "loading", HttpMethod = "GET"}, new { @class = "btn btn-default btn-lg" })

    @Ajax.ActionLink("Start","PassingTest", "Test", new {testId = Model.Id},new AjaxOptions { UpdateTargetId = "main_container", LoadingElementId = "loading", HttpMethod = "GET"@*, OnSuccess = "showFirstQuestion"*@ }, new {@class = "btn btn-success btn-lg" })
</div>

