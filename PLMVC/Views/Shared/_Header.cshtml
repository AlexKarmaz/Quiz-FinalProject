
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar_collapse_1">
            <span class="sr-only"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        @*<a class="navbar-brand">@Ajax.ActionLink("Quiz", "ShowAllTests", "Test", new AjaxOptions { UpdateTargetId = "main_container" , LoadingElementId = "loading", HttpMethod = "GET"})</a>*@
    </div>
    <div class="collapse navbar-collapse" id="navbar_collapse_1">
        <ul class="nav navbar-nav navbar-left text-center">
            @if (User.Identity.IsAuthenticated)
            {
                <li class="navbar-create">
                    @Ajax.ActionLink("Quiz", "ShowLastTests", "Test", new AjaxOptions { UpdateTargetId = "main_container", LoadingElementId = "loading", HttpMethod = "GET"})
                </li>
                <li class="navbar-create">
                    @Ajax.ActionLink("Create test","CreateTest", "Test", new AjaxOptions { UpdateTargetId = "main_container", LoadingElementId = "loading" })
                </li>
            }
        </ul>
        <ul class="nav navbar-nav navbar-right text-center">
           @if (User.Identity.IsAuthenticated)
           {
                <li>
              @using (Ajax.BeginForm("SearchTest", "Test", new AjaxOptions { UpdateTargetId = "main_container", LoadingElementId = "loading", HttpMethod = "GET" }))
              {
                    <div>
                        <p>
                            <input type="search" name="searchString" placeholder="Test search...">
                            <button class="glyphicon glyphicon-search">@*@Ajax.ActionLink("", "SearchTest","Test",new { text = SearchString })*@</button>
                        </p>
                    </div>
              }
                </li>

                <li class="navbar-create">
                    @Ajax.ActionLink("Manage", "GetAllUsers", "Account", new AjaxOptions { UpdateTargetId = "main_container", LoadingElementId = "loading" })
                </li>
           }
                <li>
                    @Html.Partial("_LoginMenu")
                </li>
</ul>
    </div>
</nav>

