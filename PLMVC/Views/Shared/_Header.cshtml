
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar_collapse_1">
            <span class="sr-only"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand">@Ajax.ActionLink("Quiz", "Index", "Home", new AjaxOptions { UpdateTargetId = "main_container" , LoadingElementId = "loading" }, htmlAttributes: new { @class = "navbar-brand" })</a>
    </div>
    <div class="collapse navbar-collapse" id="navbar_collapse_1">
        <ul class="nav navbar-nav navbar-left text-center">
            @if (User.Identity.IsAuthenticated)
            {
                <li class="navbar-create">
                    @Ajax.ActionLink("Create test","CreateTest", "Test", new AjaxOptions { UpdateTargetId = "main_container", LoadingElementId = "loading" })
                </li>
                <li class="dropdown">
                    <a class="dropdown-toggle login-dropdown" data-toggle="dropdown" role="button">Themes<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li>
                        </li>
                        <li>  
                        </li>
                    </ul>
                </li>

            }
        </ul>
        <ul class="nav navbar-nav navbar-right text-center">
            @*@if (User.Identity.IsAuthenticated)
            {
                <li>
                    @Ajax.ActionLinkWithGlyphicon("fa fa-search", "Search", "Search", "Profile", new AjaxOptions
                   {

                       UpdateTargetId = "main_container",
                       InsertionMode = InsertionMode.Replace,
                       LoadingElementId = "loading",
                       AllowCache = false,
                       OnBegin = "hideContent",
                       OnComplete = "unhideContent"

                   })
                </li>
            }*@
            <li>
                @Html.Partial("_LoginMenu")
            </li>
        </ul>
    </div>
</nav>

